import { ActiveNavbarDirective } from './active-navbar.directive';

describe('ActiveNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
