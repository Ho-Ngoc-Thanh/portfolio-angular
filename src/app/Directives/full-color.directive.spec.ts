import { FullColorDirective } from './full-color.directive';

describe('FullColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FullColorDirective();
    expect(directive).toBeTruthy();
  });
});
