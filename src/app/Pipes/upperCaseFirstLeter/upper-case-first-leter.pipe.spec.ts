import { UpperCaseFirstLeterPipe } from './upper-case-first-leter.pipe';

describe('UpperCaseFirstLeterPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperCaseFirstLeterPipe();
    expect(pipe).toBeTruthy();
  });
});
