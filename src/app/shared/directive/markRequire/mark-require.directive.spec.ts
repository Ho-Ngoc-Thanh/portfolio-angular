import { MarkRequireDirective } from './mark-require.directive';

describe('MarkRequireDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkRequireDirective();
    expect(directive).toBeTruthy();
  });
});
